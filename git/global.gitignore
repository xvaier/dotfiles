# Machine-wide only; repo-local ignores belong in that repo's .gitignore.

# Routinely holds secrets.
.envrc

# setup.sh is a per-repo hook worth sharing.
.worktrees/
!.worktrees/setup.sh

**/.claude/settings.local.json
**/.claude/.cc-writes/
